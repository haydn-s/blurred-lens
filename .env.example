# Copy to .env and fill in. .env is gitignored -- never commit real keys.

# Duke AI Gateway key, from https://dashboard.ai.duke.edu/api-keys
DUKE_AI_API_KEY=

# Only needed if config.toml points at OpenAI directly:
# OPENAI_API_KEY=
